--------------------------------------------------------------------------------------------------------
*** Обязательно сделать ***
--------------------------------------------------------------------------------------------------------
- Выбор цвета в меню СЕРВИС
- Вместо "Ом" сделать "Омега"
- При некоторых нажатиях кнопок происходит зависание. Нужно отловить, посылая через SCPI случайные нажатия
--------------------------------------------------------------------------------------------------------


+ При делителе 1/10 шаг изменения уровня синхронизации должен быть в 10 раз больше - 20мВ.                             .
+ При переключения вида измерения нужно сбрасывать режим измерения
+ Меню "Синхронизация" сделать, чтоб нормально работало
+ При режиме "Тахометр" дополнительным параметром должны быть метки.
+ Индикация фазы показывает ерунду при отстуствии сигнала на втором канале
+ После установки автоуровня сообщение остаётся на экране

2020.11.11
+ В режиме измерения частоты метки времени должны быть только в тесте

2020.11.12
+ В компараторе у выводимого значения должно быть ровно четыре знака после запятой
+ Убран выбор вида отображения статистики
+ В компараторе выбрасывать ложные точки

2020.11.13
+ Вывести прямые значения от компаратора
+ Первое же нажатие на кнопку должно вызывать действие, а не подсказку
+ Сделать счётчик времени на временные измерения (например, частота)

2020.11.30
+ Прогресс-бар TimeMeasure перекрывает окно статистики

2020.12.29
+ В "Частота-Компаратор" добавить настройкку 1/10с". Команды 0x6,5; 0x6,6
+ Добавить аппаратаный сброс - команда 0xF,6

2021.02.02
+ При включении канала С и счёта импульсов сначала выдаёт ложную настройку "время"

2021.02.04
+ Счёт импульсов должен идти строго целыми числами.
+ Проверить фазу и сделать её целым числом.
+ Тройки разрядов нужно разделить промежутками для наглядности.
+ Отрефакторить разбиение на тройки в BigFont
+ Отрефакторить разбиение на тройки при выводе целых чисел.

2021.02.05
+ Добиться того, чтобы тесты заработали на stm32
+ Сделать знак у компаратора

2021.02.06
+ Нажатие кнопки КАНАЛ должно переключать канал, нажатие кнопки РЕЖИМ должно переключать страницы настроек каналов.
+ Нулевые значения измерения должны соответствовать своим пределам (частота на 100мс - 0.01 kHz)
+ При подаче с калибратора 4.9999 МГц на вход компаратора после точки не выводится цифра




**** ---- **** БАГИ **** ---- **** -------------------------------------------------------------------------------------
- При перключении с канала С на канал А сбиваются настройки режима, если на одном канале присутствуют те параметры
  измерений (N, ...), которых нет на другом
------------------------------------------------------------------------------------------------------------------------

- SCPI не должно менять те параметры, которые отсутствуют у текущего канала, с выдачей соответствующего сообщения. На-
  пример, у канала D нет входного сопротивления.
- Индикация того, что идёт информация. Можно сделать двояким образом:
    1. Просто звёздочка со временем жизни 1 секунда, которая будет обновляться каждый раз, когда считывается новое
    значение.
    2. Через одну секунду цифры измеренния могут начинать гаснуть. На длительных измерениях с включённой памятью
    будут проблемы.

- Значения счётчиков выводить под измерением.
- Счёт импульсов Старт/Стоп нужно переделать в плане входа/выхода в режим и сделать более наглядным состояние, в
  котром находимся.
- Кнопкой "Enter" нужно только переключать состояние пункта меню. Активировать режим (Старт/Стоп, Запуск и т.д.) нужно
нажатием ручки. Выводить для этого подсказку.


*** Обязательно сделать ***

- Сделать выход из режима "счёт импульсов" - "старт/стоп"

*** Желательно сделать ***
- Прогресс-бар оставшегося времени должен быть не в виде полосы, а в виде 57/100с
- Сделать график накопленных значений для каждого измерения (открывается из меню отладки)
- В режиме ожидания данных (когда после переключения режима не пришло ни одного значения) на месте символов нужно
  выводить вращающиеся стрелки.
- Ограничить FPS 30-ю кадрами в секунду
- Сделать так, чтобы при старте загружались текущие настройки (а то через отладчик не очень удобно работать)
- Выбор шрифта для вывода измерений

- Восстановить синхронизацию
- Восстановить калибровку
- Восстановить кнопку АВТО

*** Железячные проблемы ***
- Не работает сброс
- Нет бита признака окончательного/промежуточного результата
- Два чтения, идущих один за одним, неправильно работают
