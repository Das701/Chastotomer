#include "defines.h"
#include "Heap.h"


/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#define SIZE_EMPTY_MEMORY (17 * 1024)
static uint8 emptyMemory[SIZE_EMPTY_MEMORY];


/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
void Heap::Init()
{
    volatile uint8 temp = emptyMemory[0];
    temp = temp;
}
